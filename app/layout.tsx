import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Live API Web Console - Next.js",
  description: "A Next.js-based console for using the Live API over websockets with streaming capabilities",
  keywords: ["Live API", "WebSocket", "Audio Streaming", "Video Capture", "Next.js"],
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
